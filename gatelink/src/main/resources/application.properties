quarkus.http.cors.enabled=true

# The demo UI is usually served by a different local development origin.
# Production origins should be set explicitly with QUARKUS_HTTP_CORS_ORIGINS.
%dev.quarkus.http.cors.origins=/.*/
